class Product {
  constructor(code, name, description, price, photo) {
    this.code = code;
    this.name = name;
    this.description = description;
    this.price = price;
    this.photo = photo;
  }
}

module.exports(Product);
